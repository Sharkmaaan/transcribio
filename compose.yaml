services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: transcribio
      POSTGRES_USER: transcribio_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U transcribio_user -d transcribio"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: .
    ports:
      - 8000:8000
    env_file:
      - .env
    # ADD THIS ENVIRONMENT BLOCK:
    environment:
      DATABASE_URL_SCHEMES: 'postgres=postgresql_psycopg,postgresql=postgresql_psycopg'
    # END ENVIRONMENT BLOCK
    depends_on:
      db:
        condition: service_healthy
    command: >
      sh -c "python manage.py migrate &&
             gunicorn transcribio.wsgi:application --bind=0.0.0.0:8000"

volumes:
  postgres-data: